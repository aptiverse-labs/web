"use client";

import { CommissionsTab } from "../_shared";

// The commission ledger: every accrual and reversal, and where each one stands.
export default function EarningsPage() {
  return <CommissionsTab />;
}
